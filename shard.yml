name: ladies-first-chicken-door
version: 1.0.0

dependencies:
  # The server framework
  action-controller:
    github: spider-gazelle/action-controller

  # the IO control
  gpio:
    github: spider-gazelle/gpio.cr

development_dependencies:
  ameba:
    github: veelenga/ameba
  hot_topic:
    github: jgaskins/hot_topic

# compile target
targets:
  doorctrl:
    main: src/app.cr
